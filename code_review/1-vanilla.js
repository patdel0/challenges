// For each exercise do the following:
// 1. Express your understanding of what's happening in the function
// 2. Make a prediction of what the expected output or behaviour will be
// 3. Rename the variables and functions to better represent what each variable and function does

// Snippet 1:
function bpcupi(xlj) {
  var ep_k = document.getElementById(xlj);
  if (ep_k.style.display === "none") {
    ep_k.style.display = "block";
  } else {
    ep_k.style.display = "none";
  }
}

// Snippet 2:
function copzzy() {
  var wnm = document.querySelector('input');
  var rbb = document.querySelector('div');
  wnm.textContent = rbb.textContent;
}

// Snippet 3:
function jhhyye(arr) {
  for (let hjk = 0; hjk < arr.length; hjk++) {
    console.log(arr[hjk]);
  }
}
